import 'package:auth_task/core/constants/colors.dart';
import 'package:auth_task/core/constants/strings.dart';
import 'package:flutter/material.dart';

final heading24 =
    TextStyle(fontFamily: pop_bold, fontSize: 24, color: blackColor);

final heading20 =
    TextStyle(fontFamily: pop_bold, fontSize: 20, color: blackColor);

final heading16 =
    TextStyle(fontFamily: pop_bold, fontSize: 16, color: blackColor);

final subheading24 =
    TextStyle(fontFamily: pop_regular, fontSize: 24, color: blackColor);

final subheading20 =
    TextStyle(fontFamily: pop_regular, fontSize: 20, color: blackColor);

final subheading16 =
    TextStyle(fontFamily: pop_regular, fontSize: 16, color: blackColor);
